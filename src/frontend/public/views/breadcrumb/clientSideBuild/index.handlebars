<br /><br />

This is a sample of a browser side breadcrumb built path. 

Just set view model properties like this:

<pre>
<code>
{
    "title": "Views.Breadcrumb.ClientSide.Title",
    "viewEntryPoint": "./index.js",
    "breadcrumb": true
}
</code>
</pre>


and your view entrypoint (index.js) like this


<pre>
<code>
define([
    "scripts/Template.App.ClientApp",
],
function (clientApp) {

    clientApp.view = {
        breadcrumb: function () {

            return [{
                "title": clientApp.i18n.texts.get("Back.To.Home"),
                "url": "/home/"
            }];

        },
        main: function () {

        }
    };

    return clientApp;

});
</code>
</pre>
